асинхронный код без использования сторонних библиотек можно писать тремя способами
колбэки (callback)
генераторы и корутин (generator coroutine)
с помощью синтаксиса синк_эвейт (async await)

# 3_generator.py
генераторы - это функции
yield - может быть несколько и код после выполняется при следующий функции next()
после yield - может быть другой код

# 7_asyncio_async_await
pip (pip3) install aiohttp

План:
1. Asyncio фреймворк для создания событийных циклов
2. Пример простой асинхронной программы времен Python 3.4
3. Синтаксис async/await на замену @asyncio.coroutine и yield from
4. Пример асинхронного скачивания файлов
